name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: isntall_open_gnl
      run: apt install -y libgl1-mesa-dev libglu1-mesa-dev mesa-common-dev
    - name: run_tests
      run: make test
    - name: install_valgrind
      run: sudo apt install valgrind
    - name: run_leaks_check
      run: make test-leaks
